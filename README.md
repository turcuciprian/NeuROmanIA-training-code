# Neuromania script used for training
The following script is the training script used for MRI MS leasion detection that is using ultralytics and training on the yolov8 model as the example script that can be used to replicate the training of the models
